CURRENT NEXT STEPS:
- Double check Models and AppDbContext
- Create placeholder Angular pages for /login, /dashboard
- Create backend/Services/AuthenticationService.cs


NG("/") -> GET("/api/auth/session") -> 200/401
|- 200 -> NG("/dashboard")
|- 401 -> NG("/login", note: login also handles signup)

Fresh Instance:
Angular loads /, GET /api/auth/session
 |
 V
UseAuthentication(), no Auth Cookie, HttpContext.User = Anonymous, returns 401
 |
 V
Angular routes to /login, validates inputs, HTTP POST /api/auth/login
 |
 V
POST /api/auth/login validates against DB, creates Claims, SignInAsync(),
Create Authentication Ticket, Create Cookie
 |
 V
Angular loads /, GET /api/auth/session
 |
 V
UseAuthentication(), validates Auth Cookie, rebuilds ClaimsPrincipal, 
HttpContext.User populates, return 200
 |
 V
Angular loads /dashboard
Data Flow:
- If HttpContext.User.Identity is Anonymous, /api/auth/login + /login
- If !User.IsEmailVerified, /api/auth/verify-email + /verify-email
- If User.IsEmailVerified && User.RequirePasswordChange, /api/auth/change-password + /change-password
  - If user clicks forgot-password link, /api/auth/forgot-password + /forgot-password
    - change-password requires oldPassword, newPassword, and confirmPassword
    - forgot-password required email verification to enter newPassword and confirmPassword
- If HttpContext.User.Identity && User.IsEmailVerified && !User.RequirePasswordChange, /dashboard

Order of next steps:
- email verification, forced route if IsEmailVerified == false
- change password, forced route if RequirePasswordChange == true, add button to Dashboard
- forgot password, link on login-form and change-password-form, email verification to confirm user
- OAuth2.0 (introduces whole new identity/onboarding concerns, potentially save for later)

----- NEXT STEPS -----
Update angular routing. Successful login should always route to /dashboard. Dashboard should check and route to /user/verify-email or /user/change-password if needed.
GET /api/auth/send-email-verification-code should be called when /user/verify-email is loaded (Called onInit)
GET /api/auth/send-email-verification-code expires any old codes, generates a new code, sends the email, loads the webpage
POST /api/auth/verify-email checks the submitted code and performs necessary checks. (Route Completed?, Called onSubmit)

CURRENTLY ON STEP 11 FOR IMPLEMENTING EmailVerificationService
Current Progress: I now generate a Verification Code on signup and save the HashedCode to the EmailVerificationCodes table.
Next: 
- /api/auth/resend-email-verification-code (needs to expire all other email codes for that user)
- /api/auth/verify-email (needs to accept a string to verify code. If incorrect, must expire and re-issue code)
  - need /dashboard/verify-email (has field for code + resend code link) (alert toast for incorrect code, code resent, and success) (page waits for alert to end, then erases form or re-routes to /dashboard)
Key question: Once user is logged in, how do I route the user appropriately for verifying email?
- Do I just send the email and prompt the user to verify the code immediately?